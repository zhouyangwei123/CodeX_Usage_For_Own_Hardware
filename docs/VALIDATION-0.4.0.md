# 0.4.0 验证记录

日期：2026-09-26。验证范围为 Windows 上位机、实际本机 Codex 记录/额度与模拟通讯。没有修改或烧录固件。

## 数据与界面

- 新活动统计 28 项检查通过：原始时间归位、60 秒开区间边界、重复/副本/分叉去重、同时间独立会话、未来记录排除、历史导入不制造实时峰值、无新记录的滚动到期、空闲与失败区分、截断重写、读取受阻恢复和数值保留上限。
- 50,000 个事件连续投影 20 次约 390 ms；这是合成样本性能，不代表所有磁盘/历史扫描的上限。
- 原用量 26 项回归、更新 20 项回归及配置原子保存/迁移检查通过。
- 新 HUD 77 项断言通过，覆盖折叠默认与持久化、保存失败回滚、右下角锚点、三种样式×四种尺寸×展开/折叠、透明边缘、缩放命中、隐藏停表、服务继续存在、取消订阅和 150 次绘制的 GDI 稳定性。
- 检查了 100%、75%、60% 的正式绘制器图片：底部保留 API/TOKEN/NET 三列，折叠只移除曲线；API 简写及省略部分可通过完整悬停提示查看。图片使用合成记录，不是用户桌面截图。
- 旧 HUD 87 项生命周期、84 个渲染组合、500 次原生提交及 30 次窗口循环均通过；GDI 循环前后为 18→18。保留旧入口以兼容已有调用。
- 最终 EXE 的 selfcheck、mock-test、ui-batch-selfcheck、phase-two-selfcheck 全部退出 0；更新后的设置预览也由原生离屏检查覆盖。
- 16 项 PC 协议检查通过；152 个固件文件与导入 SHA-256 清单相同，Git 固件差异为空。

## 独立审查

独立审查发现并复现一项 P2：副屏顶部展开曲线时，原有越界逻辑回退到主屏。现在优先选择右下角锚点所在屏幕，完全离屏时选最近工作区。新增并排及上下排列的回归检查；审查者另用负坐标屏幕几何复验修复。最终审查无待处理 P1/P2/P3。

## 最终 EXE 联合运行

最终 EXE 运行 **90.0345 秒**，实际 Codex 额度、PC 采样、本机记录扫描、匿名 GitHub 检查和离屏原生玻璃 HUD 同时工作，使用 MockDeviceLink。

- 3 次额度更新，预热后失效采样 0、自动重连 0。
- 421 个日志文件扫描完成，待扫描 0；Token 视图更新 10 次，固定 361 个图表端点。
- 折叠/展开两次后采集继续，原生透明合成启用，PC 帧为 28 字节。
- 本机历史存在覆盖提示，状态正确显示“部分记录”，未伪装成完整账单。
- 预热后采样 GDI 始终 24，私有内存约 76.95–77.39 MiB；该短测不能证明长期无泄漏。
- 更新检查成功；相对当时已发布的 0.3.0 返回 UpToDate。

另有多屏修复之前的 120.051 秒联合记录，4 次额度更新、13 次 Token 视图更新、无额度失效/自动重连；不把该记录当作最终 EXE 的时长。

最终 EXE SHA-256：`F6B9883C83C56B26952F68757C7A2900A778FFC1654874DDB8E27DEDECEAECD7`。

便携 ZIP SHA-256：`2FFC444FD45CFE2D697D66111BE10D2DCADF94A25056658CD6A24B810DB52CD9`。打包严格使用五个内容文件及一份 SHA 清单，独立解压后全部内容散列一致，不包含配置、认证或本机日志。

详细本地证据位于忽略的 `artifacts/token-hud/`、`artifacts/token-integrated-final/`、`artifacts/token-runtime-release/`、各模块测试目录及 `review/token-activity-review.md`。

## 边界

没有真实硬件联调、实际多屏操作/DPI切换或睡眠恢复验收；多屏位置使用纯几何及自有窗口测试。没有一小时或两小时最终版本长测，旧版长测不沿用。曲线是近实时上报用量，不是模型精确瞬时生成速度；新任务发现可能延迟约 60 秒。玻璃仍为透明底色/高光，不包含实时桌面模糊或折射。
