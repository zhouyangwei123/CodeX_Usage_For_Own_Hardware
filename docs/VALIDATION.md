# 0.2.0 验证记录

更新于 2026-09-25。当前状态：第一批代码、短时验证与最终发布 EXE 的连续两小时观察均通过。硬件现场验收仍独立延期。

## 已完成

- 18 项额度回归从失败到通过，覆盖空/稀疏/其他桶响应、身份变更、并发刷新、超时、阻塞写入、stderr 压力及初始化通知顺序。
- 100 次模拟断连恢复、100 次启动/停止通过，传输实例全部释放一次。
- 最新完整发布 EXE 构建成功，版本 0.2.0；已有自检、模拟通讯、14 项界面行为检查均通过。
- 配置备份字节一致、保存往返、失败保留原文件及临时文件清理检查通过。
- 16 项原生协议检查通过；152 个固件文件与原工程及导入清单一致。
- 900×650 和 1020×760 离屏布局已检查。没有使用桌面自动化或修改系统 DPI 设置，不将离屏检查等同于实机 DPI 验收。
- 最新发布 EXE 的 90 秒实际账户/PC/模拟设备检查通过：4 次成功额度更新，自有额度子进程停止后恢复成功，51 个 STATUS、53 个 PC_METRICS 帧，PC_METRICS 长度 28 字节。
- 实际启动位置已部署 0.2.0，旧程序与配置有本地备份，原 API Key 保持不变。

发布 EXE SHA-256：`6F41F48F3EE0FFED1EE3124174666E57767D3015C4279D9449B87CAE3B660818`。

## 连续两小时观察已完成

2026-09-25 09:29:16 至 11:29:17（UTC+8），使用上述最终 0.2.0 EXE，在独立目录进行一次全新的连续运行，实际持续 7200.42 秒，测试退出码 0。数据来自实际 Codex 账户、实际 PC 采样和 MockDeviceLink；未使用物理设备或真实 API 余额 Key。

| 检查项 | 结果 |
| --- | --- |
| 成功额度更新 | 230 次 |
| 启动后陈旧或错误采样 | 0 / 0 |
| 自动重连 | 0 次 |
| 主动停止并重启自有额度子进程 | 1 次，恢复成功 |
| 采样总数 | 721；首个启动采样尚未就绪，随后 720 次 PC 数据有效 |
| 最大采样间隔 | 13 秒，未发现超过 30 秒的间断 |
| 预热后额度最大数据年龄 | 32 秒 |
| 模拟 STATUS / PC_METRICS 帧 | 3710 / 3712；预热后的 PC_METRICS 均为 28 字节 |
| 运行结束 | 测试进程与其自有 Codex 子进程均已退出 |

测试进程第 5–15 分钟平均私有内存 60.18 MiB，最后 10 分钟平均 60.19 MiB，结束时 59.59 MiB；预热后句柄范围 670–677。累计 CPU 时间 172.58 秒，平均约为单个逻辑核心的 2.40%。这些数值针对独立测试进程，不等同于带界面和全部可选集成的托盘程序占用。

从第 9.5 分钟起另行采样的自有 Codex 子进程，私有内存范围 21.17–25.95 MiB，最大同时存在数量为 1。本次观察未见持续的资源累积；不据此推断无限期运行或其他系统环境已被验收。

本地证据保存在忽略目录 `artifacts/host-soak-20260925-final/`：`summary.json`、`samples.jsonl`、`analysis-latest.json`、`child-tree-samples.jsonl` 与 `assembly.sha256`。公开记录仅保留汇总，不上传账户记录或私人配置。

2026-09-24 的观察在用户要求暂停时结束，约 44.7 分钟、86 次更新。该段记录保留，但不计入今天的连续两小时结果。

## 发布包校验

`CodeXToolsHost-v0.2.0-windows.zip` 仅包含 EXE、中文使用说明、第三方说明和 SHA256SUMS。独立解压后的 EXE 哈希、自检与 GitHub 上传附件散列均已核对；已部署启动位置的 EXE 与被测 EXE 一致。

ZIP SHA-256：`74306CC803478C74F7BC5150BBCADFE4B6D7791481BBF1A81D9DEB9EB2F258BE`。

## 明确未验收

- 物理 USB、OLED、RGB、按键/旋钮及固件烧录：用户当前无硬件，主动延期。
- 真实断网、系统睡眠/恢复和真实多显示器 DPI 切换：本轮没有改变用户系统环境，尚未现场验收；模拟连接故障与取消恢复已覆盖。
- GitHub 自动更新、ccusage Token 清单、玻璃风格浮窗：下一阶段功能。
