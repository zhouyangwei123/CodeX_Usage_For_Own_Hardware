# 0.3.0 验证记录

日期：2026-09-25。代码、聚焦回归、整合程序和独立审查通过。范围为上位机、实际账户/本机日志及模拟通讯，不包含实机联调。

## 浮窗与界面

- 改造前 20 张基线图证明 Region 轮廓只有 0/255 透明度；改造后 3 种样式、4 种尺寸、7 种额度状态（含缺失、边界及越界）共 84 个渲染组合通过预乘 Alpha/边角/填充比例检查。
- 500 次原生更新和 50 次无效 HWND 失败调用无 GDI 资源增长；30 次窗口创建/绘制/销毁循环 GDI 18 → 18。
- 87 项生命周期断言覆盖隐藏停止动画、按需更新、刷新点击、拖动命中、透明度、数据源切换、原生失败回退和系统主题消息。没有实际改变用户系统高对比度设置。
- 设置/用量/更新页面在 1020×760、900×650 原生离屏窗口检查；最终集成 10 项检查全部通过，包括保存失败回滚、托盘变更不被旧控件覆盖、未提交预览不会丢失、关闭设置不销毁后台服务。

## 用量与更新

- 用量模块 26 项回归通过：累计和副本去重、分叉边界、半行、截断/增长重写、计数回退、取消、部分读取状态、导入 schema/费用、CSV、面板和关闭行为。
- 固定 ccusage **20.0.22** 已实际执行离线无价格命令，使用隔离合成记录。CLI 输出经正式 EXE 导入后与原生解析一致：输入含缓存 1,280,000，缓存 940,000，输出 86,000，总计 1,366,000。首次 .NET 测试时间戳格式被第三方 CLI 拒绝，改用 Codex 常见毫秒 Z 格式的合成记录后通过；未修改真实日志。
- 本机只读性能采样：约 845 MB、419 文件，26 个分段刷新合计约 8.1 秒；紧接重复刷新约 8 ms、0 字节，合计不变。数据依赖本机日志，样本有 5 类覆盖提示。该性能采样在副本选择审查修复之前，最终版另通过下述联合运行，不将其当作所有环境的上限。
- 更新模块 20 项回归通过：数值版本、正式版筛选、404/304、缓存、超时、取消、重复检查合并、调度、快速停用/启用、关闭及安全发布页链接。
- 实际匿名 GitHub 检查成功。独立 .NET EXE 的默认 TLS 问题已复现并修复，保留 SystemDefault 或补充 TLS 1.2，不要求登录。

## 程序与联合运行

- 最终 EXE 完整构建成功；原有 selfcheck、模拟通讯、第一批 UI 检查、新版 UI 检查及配置往返/原子保存均通过。
- 16 项协议测试通过；152 个固件文件与原始校验清单一致，Git 固件差异为空。
- 审查修复前 180 秒联合运行通过：实际 Codex 账户、PC 采样、真实本机用量读取、匿名更新检查、离屏原生玻璃窗口和 MockDeviceLink 同时运行，6 次额度更新，0 启动后失效/自动重连。
- 最终 EXE 再运行 90.04 秒：3 次额度更新，0 启动后失效/自动重连；419 个日志文件完成初次扫描；更新状态 UpToDate；原生透明合成启用；PC 帧为 28 字节。退出码 0。最终采样测试进程私有内存约 72.9 MiB，GDI 为 24；独立循环测试用于判断释放稳定性，单次进程初始化前后不可直接据此判定泄漏。
- 上述联合运行使用隐藏/离屏的自有窗口，不截图或操作用户桌面，不访问物理串口，不执行米家动作，也不使用真实 API 余额 Key。

最终 EXE SHA-256：`9FECC920CF0CF4EC4BA304281693E9120946D86AC0E873B5ACB04EAA89B49A42`。

便携 ZIP SHA-256：`8929524106FF376BABF47FD708964F1207DCF61CA30176419CACBCAEF2294BE9`。压缩包仅含 EXE、中文快速说明、功能说明、第三方说明和散列清单；独立解压后四个内容文件的散列全部一致。原启动位置已备份并更新到相同 EXE，配置迁移到版本 10；原有非版本/位置字段逐项比较保持一致，包括密钥和绑定。默认外观为 glass。

本地完整证据保存在忽略的 `artifacts/phase-two/`、`artifacts/phase-two-runtime-release/`、`artifacts/ccusage-comparison/` 和各模块测试目录。独立审查曾发现 3 项 P2、2 项 P3，均用原始复现程序确认修复，无未解决项。公开仓库只保存汇总和合成测试，不上传用户日志或配置。

## 未验收与含义限制

- 固件未修改、未烧录；OLED/RGB/按键/USB 物理链路仍待硬件到手。
- 真实多显示器 DPI 切换、高对比度操作、系统睡眠/恢复及长时间 Token 扫描未现场验收。
- 玻璃风格不是实时桌面模糊或折射；有不透明回退。
- 本机 Token 与导入报告不等于全账户账单；真实全历史与 ccusage/官方账户统计未逐项对账。
- 12 小时更新周期用可控时钟和定时器验证，没有宣称进行了 12 小时实测。
- 0.2.0 的连续两小时额度观察记录在 `VALIDATION.md`；本轮没有将该时长冒充为 0.3.0 的长测。
